{
  "name": "qatrail",
  "version": "2.2.2",
  "private": true,
  "description": "QA reporting: App/Project -> Feature -> Test Case -> Record -> Issue, with review workflow, SLA, analytics",
  "author": "Aulia Akbar Harahap",
  "createdAt": "2026-07",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm -w server run dev",
    "dev:client": "npm -w client run dev",
    "build": "npm -w server run build && npm -w client run build",
    "release": "./scripts/version.sh",
    "start": "npm -w server run start",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:push": "npm -w server run db:push",
    "db:seed": "npm -w server run seed",
    "db:seed:content": "npm -w server run seed:content",
    "setup": "npm install && npm run db:up && npm run db:push && npm run db:seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
